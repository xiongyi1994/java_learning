package nio;

public class BufferLearning {
	/*
	 * Buffer(缓冲区) 是一个线性的、有序的数据集，只能容纳某种特定的数据类型
	 * 
	 * Buffer类常用方法：
	 * capacity()
	 * limit()
	 * position()
	 * clear()
	 * flip() 重设缓冲区，在写入之前调用，改变缓冲的指针
	 * reset()
	 * hasRemaining()
	 * 
	 * 不同类型的缓冲区：
	 * ByteBuffer、CharBuffer、ShortBuffer、IntBuffer、
	 * LongBuffer、FloatBuffer、DoubleBuffer
	 * 
	 * 各种数据类型缓冲区提供的常用的方法：
	 * allocate()
	 * get()
	 * put()
	 * slice()
	 * asReadOnlyBuffer()
	 * 
	 * */
}
